import { createUseStyles } from 'react-jss';

const styles = {
  'certificates-container': {
    'maxWidth': '100vw',
    'overflowX': 'hidden',
    'padding': '16px 24px',
    'width': '100vw'
  },
  'candidate-name': {
    'fontWeight': 500
  }
};

export default createUseStyles(styles);

import { createUseStyles } from 'react-jss';

const styles = {
  'container': {
    'alignItems': 'center',
    'display': 'flex',
    'height': '100vh',
    'justifyContent': 'center',
    'marginTop': '-20px'
  }
}

export default createUseStyles(styles);

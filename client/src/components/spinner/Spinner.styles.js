import { createUseStyles } from 'react-jss';

const styles = {
  'spinner-container': {
    'alignItems': 'center',
    'display': 'flex',
    'height': '100vh',
    'justifyContent': 'center'
  }
}

export default createUseStyles(styles);
